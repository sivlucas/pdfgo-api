[project]
name = "pdfgo-api"
version = "1.0.0"
description = "API profissional para manipulação de PDFs"
authors = [
    {name = "PDFGo Team", email = "suporte@pdfgo.app"},
]
readme = "README.md"
requires-python = ">=3.11"
keywords = ["pdf", "api", "conversion", "manipulation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://pdfgo.app"
Documentation = "https://docs.pdfgo.app"
Repository = "https://github.com/pdfgo/api"
"Issue Tracker" = "https://github.com/pdfgo/api/issues"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
target-version = ['py311']

[tool.isort]
profile = "black"
multi_line_output = 3

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
asyncio_mode = "auto"