# Servidor
HOST=0.0.0.0
PORT=8000
DEBUG=False

# Armazenamento
MAX_FILE_SIZE=104857600  # 100MB em bytes
UPLOAD_DIR=storage/uploads
OUTPUT_DIR=storage/outputs
TEMP_DIR=storage/temp

# Processamento
OCR_ENABLED=True
DEFAULT_DPI=300
MAX_PAGES_FOR_ANALYSIS=50

# Qualidade
MIN_QUALITY_SCORE=0.7

# Redis
REDIS_URL=redis://localhost:6379

# CORS
CORS_ORIGINS=*

# Segurança (gerar uma chave secreta para produção)
SECRET_KEY=pdfgo-secret-key-change-in-production

# Sentry (opcional)
SENTRY_DSN=